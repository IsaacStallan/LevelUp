[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd src/backend && npm ci --omit=dev"]

[phases.build]
cmds = ["echo 'Backend — no build step'"]

[start]
cmd = "cd src/backend && node src/index.js"
