[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd src/backend && npm install --omit=dev"]

[phases.build]
cmds = ["echo 'Backend — no build step'"]

[start]
cmd = "node /app/src/backend/src/index.js"
